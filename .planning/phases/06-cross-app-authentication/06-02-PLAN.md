---
phase: 06-cross-app-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/generator/src/routes/auth.$authView.tsx
  - apps/trucking/src/routes/auth.$authView.tsx
  - apps/generator/src/routes/index.tsx
  - apps/trucking/src/routes/index.tsx
autonomous: true

must_haves:
  truths:
    - "User can sign in from generator app (port 3001)"
    - "User can sign in from trucking app (port 3003)"
    - "Sign-in redirects to dashboard on same app initially"
    - "Post-login org-type routing kicks in on dashboard navigation"
  artifacts:
    - path: "apps/generator/src/routes/auth.$authView.tsx"
      provides: "Sign-in page for generator app"
      contains: "AuthView"
    - path: "apps/trucking/src/routes/auth.$authView.tsx"
      provides: "Sign-in page for trucking app"
      contains: "AuthView"
  key_links:
    - from: "apps/generator/src/routes/auth.$authView.tsx"
      to: "@daveyplate/better-auth-ui"
      via: "import { AuthView }"
      pattern: "AuthView"
---

<objective>
Ensure sign-in pages on generator and trucking apps work correctly with cross-app authentication.

Purpose: Implements XAUTH-01 (session works on generator app) and XAUTH-03 (session works on trucking app). Users should be able to sign in from any app and have their session recognized.

Output: Generator and trucking apps have working sign-in flows that leverage the shared Better Auth session.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase research
@.planning/phases/06-cross-app-authentication/06-RESEARCH.md

# Existing auth pages (reference pattern)
@apps/treater/src/routes/auth.$authView.tsx
@apps/generator/src/routes/auth.$authView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update generator app auth routes</name>
  <files>
    apps/generator/src/routes/auth.$authView.tsx
    apps/generator/src/routes/index.tsx
  </files>
  <action>
Verify the generator app's auth routes are correctly configured. They were set up in Phase 4 (04-01) but verify they match the treater pattern.

Check `auth.$authView.tsx`:
- Uses AuthView from @daveyplate/better-auth-ui
- Has beforeLoad that redirects authenticated users to /dashboard
- Passes redirectTo="/dashboard" to AuthView

Check `index.tsx`:
- Redirects to /dashboard if authenticated
- Redirects to /auth/sign-in if not authenticated

If any differences from treater pattern, align them. The key pattern should be:

```typescript
// auth.$authView.tsx beforeLoad
beforeLoad: async ({ context }) => {
  if (context.isAuthenticated) {
    throw redirect({ to: "/dashboard" });
  }
}
```

AuthView should use the same pattern as treater:
```typescript
<AuthView
  view={authView as "sign-in" | "sign-up" | "forgot-password" | "reset-password"}
  redirectTo="/dashboard"
/>
```
  </action>
  <verify>
1. Run `pnpm typecheck` - should pass
2. Navigate to `http://localhost:3001/auth/sign-in` - should show sign-in form
3. If already authenticated, should redirect to `/dashboard`
  </verify>
  <done>
Generator app auth routes verified and working correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update trucking app auth routes</name>
  <files>
    apps/trucking/src/routes/auth.$authView.tsx
    apps/trucking/src/routes/index.tsx
  </files>
  <action>
Verify the trucking app's auth routes are correctly configured. Same verification as Task 1 but for trucking app.

Check `auth.$authView.tsx`:
- Uses AuthView from @daveyplate/better-auth-ui
- Has beforeLoad that redirects authenticated users to /dashboard
- Passes redirectTo="/dashboard" to AuthView

Check `index.tsx`:
- Redirects to /dashboard if authenticated
- Redirects to /auth/sign-in if not authenticated

If any differences from treater pattern, align them.
  </action>
  <verify>
1. Run `pnpm typecheck` - should pass
2. Navigate to `http://localhost:3003/auth/sign-in` - should show sign-in form
3. If already authenticated, should redirect to `/dashboard`
  </verify>
  <done>
Trucking app auth routes verified and working correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Test cross-app session sharing</name>
  <files></files>
  <action>
This is a verification-only task. No code changes needed.

Test the cross-app session:
1. Clear all cookies for localhost
2. Start all apps with `pnpm dev`
3. Go to `http://localhost:3002/auth/sign-in` (treater)
4. Sign in with a test account
5. Without signing in again:
   - Navigate to `http://localhost:3001` (generator) - should recognize session
   - Navigate to `http://localhost:3003` (trucking) - should recognize session

If session is NOT recognized on other apps:
- Check browser DevTools > Application > Cookies - verify `better-auth.session_token` cookie exists
- Check Network tab for any auth errors
- Verify trustedOrigins in packages/convex/convex/auth.ts includes all three localhost ports

Document any issues found for gap closure.
  </action>
  <verify>
Session is shared: Sign in on treater, recognized on generator and trucking without re-signing in
  </verify>
  <done>
Cross-app session sharing verified working (or issues documented for gap closure)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `pnpm typecheck` passes
2. All apps have working sign-in pages
3. Session is shared across all three apps
4. Authenticated users redirected to dashboard from auth pages
5. Unauthenticated users redirected to sign-in from protected pages
</verification>

<success_criteria>
1. Sign in on treater app -> session recognized on generator app (XAUTH-01)
2. Sign in on treater app -> session recognized on trucking app (XAUTH-03)
3. Sign in on generator app -> session recognized on treater app (XAUTH-02)
4. Auth pages work correctly on all three apps
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-app-authentication/06-02-SUMMARY.md`
</output>
