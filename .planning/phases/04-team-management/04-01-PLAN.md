---
phase: 04-team-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/generator/src/lib/auth-server.ts
  - apps/generator/src/routes/api/auth/$.ts
  - apps/generator/src/routes/__root.tsx
  - apps/generator/src/router.tsx
  - apps/trucking/src/lib/auth-server.ts
  - apps/trucking/src/routes/api/auth/$.ts
  - apps/trucking/src/routes/__root.tsx
  - apps/trucking/src/router.tsx
autonomous: true

must_haves:
  truths:
    - "Generator user can authenticate via generator app (port 3001)"
    - "Trucking user can authenticate via trucking app (port 3003)"
    - "Auth state persists across page refresh in generator app"
    - "Auth state persists across page refresh in trucking app"
  artifacts:
    - path: "apps/generator/src/lib/auth-server.ts"
      provides: "SSR auth helpers for generator app"
      contains: "convexBetterAuthReactStart"
    - path: "apps/generator/src/routes/api/auth/$.ts"
      provides: "Auth proxy route for generator app"
      exports: ["GET", "POST"]
    - path: "apps/trucking/src/lib/auth-server.ts"
      provides: "SSR auth helpers for trucking app"
      contains: "convexBetterAuthReactStart"
    - path: "apps/trucking/src/routes/api/auth/$.ts"
      provides: "Auth proxy route for trucking app"
      exports: ["GET", "POST"]
  key_links:
    - from: "apps/generator/src/routes/__root.tsx"
      to: "ConvexBetterAuthProvider"
      via: "provider wrapper in RootComponent"
      pattern: "ConvexBetterAuthProvider"
    - from: "apps/trucking/src/routes/__root.tsx"
      to: "ConvexBetterAuthProvider"
      via: "provider wrapper in RootComponent"
      pattern: "ConvexBetterAuthProvider"
---

<objective>
Enable full SSR authentication in generator (port 3001) and trucking (port 3003) apps.

Purpose: Generator and hauler users need to authenticate via their respective apps. Currently these apps have auth clients but lack the SSR infrastructure that the treater app has (auth-server.ts, api proxy route, ConvexBetterAuthProvider).

Output: Both apps can authenticate users with session persistence, matching treater app's auth capabilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/summary.md
@.planning/phases/04-team-management/04-RESEARCH.md

# Reference implementation (treater app)
@apps/treater/src/lib/auth-server.ts
@apps/treater/src/routes/api/auth/$.ts
@apps/treater/src/routes/__root.tsx
@apps/treater/src/router.tsx

# Target apps (current state)
@apps/generator/src/routes/__root.tsx
@apps/generator/src/lib/auth.ts
@apps/trucking/src/routes/__root.tsx
@apps/trucking/src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SSR auth infrastructure to generator app</name>
  <files>
    apps/generator/src/lib/auth-server.ts
    apps/generator/src/routes/api/auth/$.ts
    apps/generator/src/routes/__root.tsx
  </files>
  <action>
Copy the SSR auth pattern from treater app to generator app:

1. Create `apps/generator/src/lib/auth-server.ts`:
   - Copy exact content from `apps/treater/src/lib/auth-server.ts`
   - Uses `convexBetterAuthReactStart` from `@convex-dev/better-auth/react-start`
   - Exports: handler, getToken, fetchAuthQuery, fetchAuthMutation, fetchAuthAction
   - Uses `process.env.VITE_CONVEX_URL` and `process.env.VITE_CONVEX_SITE_URL`

2. Create `apps/generator/src/routes/api/auth/$.ts`:
   - Copy pattern from `apps/treater/src/routes/api/auth/$.ts`
   - Import handler from `@/lib/auth-server`
   - Export GET and POST functions that call handler(request)
   - Note: Use regular function exports, not createAPIFileRoute (check treater's actual pattern)

3. Update `apps/generator/src/routes/__root.tsx`:
   - Change from `createRootRoute` to `createRootRouteWithContext<RouterContext>`
   - Add `getToken` import from `@/lib/auth-server`
   - Add `createServerFn` import from `@tanstack/react-start`
   - Create `getAuth` server function that calls `getToken()`
   - Add `beforeLoad` that gets token and sets auth on convexQueryClient.serverHttpClient
   - Return `{ isAuthenticated: !!token, token }` from beforeLoad
   - Import ConvexBetterAuthProvider from `@convex-dev/better-auth/react`
   - Import authClient from `@/lib/auth`
   - Import AuthProvider from `@/contexts/auth-context`
   - Wrap Outlet in ConvexBetterAuthProvider and AuthProvider in RootComponent
   - Get convexClient from router.options.context?.convexQueryClient?.convexClient

Key differences from current generator __root.tsx:
- Current: Simple createRootRoute with no auth
- New: createRootRouteWithContext with beforeLoad, server functions, and provider wrapping
  </action>
  <verify>
Run `pnpm --filter @hwm/generator typecheck` - should pass with no errors.
Verify files exist: `ls apps/generator/src/lib/auth-server.ts apps/generator/src/routes/api/auth/$.ts`
  </verify>
  <done>
Generator app has auth-server.ts, api/auth/$.ts route, and __root.tsx with ConvexBetterAuthProvider.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SSR auth infrastructure to trucking app</name>
  <files>
    apps/trucking/src/lib/auth-server.ts
    apps/trucking/src/routes/api/auth/$.ts
    apps/trucking/src/routes/__root.tsx
  </files>
  <action>
Copy the SSR auth pattern from treater app to trucking app:

1. Create `apps/trucking/src/lib/auth-server.ts`:
   - Copy exact content from `apps/treater/src/lib/auth-server.ts`
   - Same exports: handler, getToken, fetchAuthQuery, fetchAuthMutation, fetchAuthAction

2. Create `apps/trucking/src/routes/api/auth/$.ts`:
   - Copy pattern from `apps/treater/src/routes/api/auth/$.ts`
   - Import handler from `@/lib/auth-server`
   - Export GET and POST functions

3. Update `apps/trucking/src/routes/__root.tsx`:
   - Same transformation as generator app
   - Change to `createRootRouteWithContext<RouterContext>`
   - Add server function for getAuth
   - Add beforeLoad for token handling
   - Wrap in ConvexBetterAuthProvider and AuthProvider
   - Import required modules (ConvexBetterAuthProvider, authClient, AuthProvider, etc.)

Note: The trucking app's current __root.tsx and auth-context.tsx should be nearly identical to generator's, so the same transformation applies.
  </action>
  <verify>
Run `pnpm --filter @hwm/trucking typecheck` - should pass with no errors.
Verify files exist: `ls apps/trucking/src/lib/auth-server.ts apps/trucking/src/routes/api/auth/$.ts`
  </verify>
  <done>
Trucking app has auth-server.ts, api/auth/$.ts route, and __root.tsx with ConvexBetterAuthProvider.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update router.tsx files for SSR auth context</name>
  <files>
    apps/generator/src/router.tsx
    apps/trucking/src/router.tsx
  </files>
  <action>
Update both router.tsx files to match treater's pattern for providing convexQueryClient in context:

1. Check `apps/treater/src/router.tsx` for the pattern used
2. Ensure generator and trucking routers:
   - Create RouterContext interface with queryClient and convexQueryClient
   - Pass convexQueryClient to router context so __root.tsx can access it
   - This enables the beforeLoad to call context.convexQueryClient.serverHttpClient.setAuth(token)

If router.tsx files already have the correct pattern (likely from earlier phases), verify they match treater's.

Common pattern needed:
```typescript
interface RouterContext {
  queryClient: QueryClient;
  convexQueryClient: ConvexQueryClient;
}

// In router creation:
context: {
  queryClient,
  convexQueryClient,
}
```
  </action>
  <verify>
Run `pnpm typecheck` from root - all three apps should typecheck successfully.
  </verify>
  <done>
Both generator and trucking routers provide convexQueryClient in context for SSR auth.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes for all apps
2. Start generator app: `pnpm --filter @hwm/generator dev`
   - Visit http://localhost:3001/auth/sign-in
   - Auth UI should render
3. Start trucking app: `pnpm --filter @hwm/trucking dev`
   - Visit http://localhost:3003/auth/sign-in
   - Auth UI should render
4. Both apps have the same auth file structure as treater:
   - lib/auth.ts (already exists)
   - lib/auth-server.ts (new)
   - routes/api/auth/$.ts (new)
   - routes/__root.tsx (updated)
   - contexts/auth-context.tsx (already exists)
</verification>

<success_criteria>
- Generator app at port 3001 can render auth pages
- Trucking app at port 3003 can render auth pages
- SSR auth token is retrieved and passed to ConvexBetterAuthProvider
- Auth state (isAuthenticated, token) available in route context
- TypeScript compilation passes for both apps
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-management/04-01-SUMMARY.md`
</output>
