---
phase: 01-core-authentication
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/treater/src/components/login-page.tsx
  - apps/treater/src/components/dashboard/generators-overview.tsx
  - apps/treater/src/lib/mock-data.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript passes with no auth-related errors"
    - "No dead code referencing old auth API"
    - "MockGenerator type export works for consumers"
  artifacts:
    - path: "apps/treater/src/lib/mock-data.ts"
      provides: "MockGenerator type alias export"
      contains: "export type MockGenerator"
    - path: "apps/treater/src/components/dashboard/generators-overview.tsx"
      provides: "Correctly typed generator component"
      contains: "MockGenerator"
  key_links:
    - from: "apps/treater/src/components/dashboard/generators-overview.tsx"
      to: "apps/treater/src/lib/mock-data.ts"
      via: "import MockGenerator"
      pattern: "import.*MockGenerator.*from.*mock-data"
---

<objective>
Clean up dead code and fix type export issue to resolve remaining TypeScript errors.

Purpose: The login-page.tsx component is dead code (not imported anywhere) that uses the old auth API. The generators-overview.tsx imports MockGenerator but it's now TreaterMockGenerator. Fix both to achieve clean TypeScript compilation.

Output: No TypeScript errors related to auth API mismatch or type imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-authentication/01-VERIFICATION.md

# Files to modify
@apps/treater/src/components/login-page.tsx
@apps/treater/src/components/dashboard/generators-overview.tsx
@apps/treater/src/lib/mock-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead login-page.tsx component</name>
  <files>apps/treater/src/components/login-page.tsx</files>
  <action>
Delete the file apps/treater/src/components/login-page.tsx.

Rationale:
- This component is NOT imported anywhere in the codebase (grep confirms no imports)
- The auth UI is now handled by @daveyplate/better-auth-ui's AuthView at /auth/$authView
- The component uses the old auth API (login()) that no longer exists
- Keeping dead code that references non-existent APIs causes TypeScript errors

This is a clean deletion — no other files reference it.
  </action>
  <verify>
File no longer exists:
```bash
test ! -f apps/treater/src/components/login-page.tsx && echo "Deleted"
```
  </verify>
  <done>
login-page.tsx deleted. TypeScript error "Property 'login' does not exist" eliminated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MockGenerator type alias in mock-data.ts</name>
  <files>apps/treater/src/lib/mock-data.ts, apps/treater/src/components/dashboard/generators-overview.tsx</files>
  <action>
The generators-overview.tsx imports `MockGenerator` but mock-data.ts exports `TreaterMockGenerator`.

Fix by adding a type alias in mock-data.ts:

1. Add after the TreaterMockGenerator interface (around line 36):
   ```typescript
   // Alias for backward compatibility with components expecting MockGenerator
   export type MockGenerator = TreaterMockGenerator;
   ```

2. Update the export line (around line 101) to also export the alias:
   - The mockGenerators array is already typed as TreaterMockGenerator[]
   - The alias allows consumers to import either name

Alternative approach (if cleaner): Update generators-overview.tsx to import TreaterMockGenerator instead:
- Change `type MockGenerator` to `type TreaterMockGenerator`
- Update all usages in the file

Choose the approach that minimizes changes. Since generators-overview.tsx already uses MockGenerator extensively (5+ usages), adding the alias is cleaner.
  </action>
  <verify>
Run `pnpm --filter @hwm/treater typecheck` — no MockGenerator-related errors.
Grep for "MockGenerator" in mock-data.ts confirms export exists.
  </verify>
  <done>
MockGenerator type is exported from mock-data.ts. generators-overview.tsx compiles without type errors.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Full TypeScript check passes:
   ```bash
   pnpm --filter @hwm/treater typecheck
   ```
   Should complete with no errors (or only pre-existing unrelated errors).

2. Verify login-page.tsx is gone:
   ```bash
   ls apps/treater/src/components/login-page.tsx 2>&1 | grep -q "No such file"
   ```

3. Verify MockGenerator export:
   ```bash
   grep "export type MockGenerator" apps/treater/src/lib/mock-data.ts
   ```
</verification>

<success_criteria>
- login-page.tsx deleted (dead code removed)
- MockGenerator type exported from mock-data.ts
- generators-overview.tsx compiles without errors
- No TypeScript errors related to auth API or type imports
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-authentication/01-05-SUMMARY.md`
</output>
