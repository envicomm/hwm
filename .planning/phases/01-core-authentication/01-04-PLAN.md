---
phase: 01-core-authentication
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/treater/src/routes/__root.tsx
  - apps/treater/src/components/layout/header.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "useAuth() returns auth state without throwing"
    - "Header displays user name and email from session"
    - "Sign out button triggers signOut and redirects to home"
  artifacts:
    - path: "apps/treater/src/routes/__root.tsx"
      provides: "AuthProvider wrapping application"
      contains: "<AuthProvider>"
    - path: "apps/treater/src/components/layout/header.tsx"
      provides: "Header with correct auth API usage"
      contains: "signOut"
  key_links:
    - from: "apps/treater/src/routes/__root.tsx"
      to: "AuthProvider"
      via: "import and wrap"
      pattern: "AuthProvider.*children"
    - from: "apps/treater/src/components/layout/header.tsx"
      to: "useAuth().signOut"
      via: "destructuring"
      pattern: "signOut.*useAuth"
---

<objective>
Wire AuthProvider into the component tree and update Header to use the new Better Auth API.

Purpose: Close the critical gap where AuthProvider is defined but not used, causing useAuth() to throw at runtime. Also fix Header component which uses old API (logout, treaterName) that no longer exists.

Output: Working auth context available throughout the app, Header component correctly displays user info and handles sign out.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-authentication/01-VERIFICATION.md

# Files to modify
@apps/treater/src/routes/__root.tsx
@apps/treater/src/components/layout/header.tsx
@apps/treater/src/contexts/auth-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AuthProvider into __root.tsx</name>
  <files>apps/treater/src/routes/__root.tsx</files>
  <action>
Update __root.tsx to wrap the Outlet with AuthProvider from auth-context.tsx:

1. Add import: `import { AuthProvider } from "@/contexts/auth-context";`

2. In RootComponent, wrap Outlet with AuthProvider INSIDE ConvexBetterAuthProvider:
   ```tsx
   <ConvexBetterAuthProvider ...>
     <AuthProvider>
       <Outlet />
     </AuthProvider>
   </ConvexBetterAuthProvider>
   ```

Note: AuthProvider must be INSIDE ConvexBetterAuthProvider because it uses useConvexAuth() which requires the Convex context.

3. Also wrap the fallback case (no convex client) with AuthProvider for consistency, though this path shouldn't happen in practice.
  </action>
  <verify>
Run `pnpm --filter @hwm/treater typecheck` — no errors related to AuthProvider import.
Grep for "AuthProvider" in __root.tsx confirms it's imported and used.
  </verify>
  <done>
AuthProvider wraps Outlet inside ConvexBetterAuthProvider. useAuth() will no longer throw.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Header to use new auth API</name>
  <files>apps/treater/src/components/layout/header.tsx</files>
  <action>
Update header.tsx to use the correct Better Auth API:

1. Change destructuring from `const { user, logout } = useAuth()` to:
   ```tsx
   const { user, signOut } = useAuth();
   ```

2. Update handleLogout function:
   - Change `logout()` to `await signOut()` (signOut is async)
   - Make handleLogout async:
   ```tsx
   const handleLogout = async () => {
     await signOut();
     navigate({ to: "/" });
   };
   ```

3. Remove treaterName reference:
   - Line 34 shows `{user?.treaterName}` — this property doesn't exist on AuthUser
   - Replace with a static label or remove (treaterName will be added in Phase 2 when organization bridge is implemented)
   - For now, change to: `<h1 className="text-lg font-semibold">Treatment Facility</h1>`

4. Fix initials calculation:
   - Current code assumes user.name exists but AuthUser has `name: string | null`
   - Update to handle null: `const initials = user?.name?.split(" ")...` with fallback
  </action>
  <verify>
Run `pnpm --filter @hwm/treater typecheck` — no errors in header.tsx.
Grep for "logout" in header.tsx returns no results.
Grep for "treaterName" in header.tsx returns no results.
  </verify>
  <done>
Header uses signOut() instead of logout(), doesn't reference treaterName, handles null user.name gracefully.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. TypeScript check passes for treater app:
   ```bash
   pnpm --filter @hwm/treater typecheck
   ```

2. Auth-related errors should be resolved:
   - No "Property 'logout' does not exist" error
   - No "Property 'treaterName' does not exist" error

3. Verify AuthProvider is wired:
   ```bash
   grep -n "AuthProvider" apps/treater/src/routes/__root.tsx
   ```
   Should show import and usage.
</verification>

<success_criteria>
- AuthProvider wraps the application inside ConvexBetterAuthProvider
- Header uses signOut (not logout)
- Header doesn't reference treaterName
- TypeScript passes without auth-related errors in these files
- useAuth() hook works at runtime (no "must be used within AuthProvider" error)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-authentication/01-04-SUMMARY.md`
</output>
