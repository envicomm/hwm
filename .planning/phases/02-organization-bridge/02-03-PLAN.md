---
phase: 02-organization-bridge
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - packages/convex/convex/organizations/mutations.ts
  - packages/convex/convex/organizations/index.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Creating treater atomically creates domain entity + Better Auth org + link"
    - "Creating generator atomically creates domain entity + Better Auth org + link with parent"
    - "Creating hauler atomically creates domain entity + Better Auth org + link with parent"
    - "Organization links correctly track parent hierarchy"
  artifacts:
    - path: "packages/convex/convex/organizations/mutations.ts"
      provides: "Atomic organization creation mutations"
      exports: ["createTreaterWithOrganization", "createGeneratorWithOrganization", "createHaulerWithOrganization"]
  key_links:
    - from: "packages/convex/convex/organizations/mutations.ts"
      to: "Better Auth auth.api.createOrganization"
      via: "authComponent.getAuth pattern"
      pattern: "auth\\.api\\.createOrganization"
    - from: "packages/convex/convex/organizations/mutations.ts"
      to: "organizationLinks table"
      via: "ctx.db.insert"
      pattern: "ctx\\.db\\.insert.*organizationLinks"
---

<objective>
Create atomic organization creation mutations.

Purpose: Implement the core mutations that create HWM domain entities (treaters, generators, haulers) with their corresponding Better Auth organizations and bridge links in a single atomic operation.

Output: Three mutations that ensure domain entity + Better Auth org + link are created together or all fail.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-organization-bridge/02-CONTEXT.md
@.planning/phases/02-organization-bridge/02-RESEARCH.md

# Reference auth setup and patterns
@packages/convex/convex/auth.ts
@packages/convex/convex/schema/treaters.ts
@packages/convex/convex/schema/generators.ts
@packages/convex/convex/schema/haulers.ts
@packages/convex/convex/schema/validators.ts

# Use these helpers
@packages/convex/convex/organizations/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create createTreaterWithOrganization mutation</name>
  <files>packages/convex/convex/organizations/mutations.ts</files>
  <action>
Create `packages/convex/convex/organizations/mutations.ts` with treater mutation:

**createTreaterWithOrganization**:
- Args: name, address, contactEmail, contactPhone, licenseNumber (optional)
- Handler:
  1. Get auth context: `const { auth, headers } = await authComponent.getAuth(createAuth, ctx)`
  2. Generate slug: `args.name.toLowerCase().replace(/[^a-z0-9]+/g, "-")`
  3. Call `auth.api.createOrganization` with metadata `{ organizationType: "treater" }`
  4. Create treater in Convex db with isActive: true
  5. Create organizationLink with treaterId, no parentBetterAuthOrgId (top-level)
  6. Return { treaterId, betterAuthOrgId }

Use `makeOrgLinkData` helper for type-safe link creation.
Throw ConvexError if Better Auth org creation fails.

Note from RESEARCH.md: If Convex mutation fails after Better Auth org created, there will be an orphan. Accept for MVP, add TODO comment about cleanup job.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - should pass
Function signature matches expected args
  </verify>
  <done>
createTreaterWithOrganization mutation creates treater + Better Auth org + link atomically
  </done>
</task>

<task type="auto">
  <name>Task 2: Add createGeneratorWithOrganization and createHaulerWithOrganization mutations</name>
  <files>packages/convex/convex/organizations/mutations.ts</files>
  <action>
Add two more mutations to organizations/mutations.ts:

**createGeneratorWithOrganization**:
- Args: treaterId, name, address, contactEmail, contactPhone, facilityCode (optional), qrMode
- Handler:
  1. Get auth context
  2. Get parent treater's org link using `getBetterAuthOrgFromEntity`
  3. Generate slug
  4. Create Better Auth org with metadata `{ organizationType: "generator", parentTreaterId: args.treaterId }`
  5. Create generator with treaterId reference
  6. Create organizationLink with generatorId + parentBetterAuthOrgId (treater's Better Auth org ID)
  7. Return { generatorId, betterAuthOrgId }

**createHaulerWithOrganization**:
- Args: treaterId, name, address, contactEmail, contactPhone, licenseNumber (optional)
- Handler:
  1. Get auth context
  2. Get parent treater's org link
  3. Generate slug
  4. Create Better Auth org with metadata `{ organizationType: "hauler", parentTreaterId: args.treaterId }`
  5. Create hauler entity (note: haulers table doesn't have treaterId - hauler-treater relationship is via treaterHaulerPartners)
  6. Create organizationLink with haulerId + parentBetterAuthOrgId
  7. Create treaterHaulerPartners record to establish relationship
  8. Return { haulerId, betterAuthOrgId }

Import qrMode validator from schema/validators.ts for generator mutation.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - should pass
Both functions properly reference parent org
  </verify>
  <done>
Generator and hauler mutations create child orgs with parent hierarchy tracking
  </done>
</task>

<task type="auto">
  <name>Task 3: Update barrel export and verify</name>
  <files>packages/convex/convex/organizations/index.ts</files>
  <action>
Update organizations/index.ts to export mutations:

```typescript
export * from "./helpers";
export * from "./mutations";
```

Then verify the full module works:
1. Run typecheck
2. Run `pnpm --filter @hwm/convex dev` to sync functions

All three mutations should be available in the Convex API.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - passes
Run `pnpm --filter @hwm/convex dev` - syncs without errors
Check Convex dashboard shows new functions
  </verify>
  <done>
All organization mutations exported and deployed to Convex
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @hwm/convex typecheck` passes
2. `pnpm --filter @hwm/convex dev` syncs successfully
3. Three mutations available: createTreaterWithOrganization, createGeneratorWithOrganization, createHaulerWithOrganization
4. Each mutation:
   - Calls auth.api.createOrganization
   - Creates domain entity
   - Creates organizationLink with correct parent reference
</verification>

<success_criteria>
- All three mutations implemented with correct args
- Better Auth org creation uses authComponent.getAuth pattern
- organizationLinks created with parentBetterAuthOrgId for child orgs
- treaterHaulerPartners record created for haulers
- TypeScript compiles without errors
- Mutations deployed to Convex
</success_criteria>

<output>
After completion, create `.planning/phases/02-organization-bridge/02-03-SUMMARY.md`
</output>
