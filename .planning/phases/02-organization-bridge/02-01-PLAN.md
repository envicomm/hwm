---
phase: 02-organization-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/convex/convex/schema/organizationLinks.ts
  - packages/convex/convex/schema/users.ts
  - packages/convex/convex/schema/index.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "organizationLinks table has parentBetterAuthOrgId field for hierarchy tracking"
    - "users table has betterAuthUserId field for linking to Better Auth"
    - "Convex schema deploys without errors"
  artifacts:
    - path: "packages/convex/convex/schema/organizationLinks.ts"
      provides: "organizationLinks with parentBetterAuthOrgId + by_parent_org index"
      contains: "parentBetterAuthOrgId"
    - path: "packages/convex/convex/schema/users.ts"
      provides: "users with betterAuthUserId + by_better_auth_user index"
      contains: "betterAuthUserId"
  key_links:
    - from: "packages/convex/convex/schema/index.ts"
      to: "organizationLinks, users"
      via: "export statement"
      pattern: "export.*organizationLinks|export.*users"
---

<objective>
Update Convex schema to support organization bridge pattern.

Purpose: Enable linking Better Auth organizations to HWM domain entities (treaters, generators, haulers) and linking Better Auth users to Convex users table. These schema changes are prerequisites for all organization mutations.

Output: Updated schema files with new fields and indexes, deployed to Convex.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-organization-bridge/02-CONTEXT.md
@.planning/phases/02-organization-bridge/02-RESEARCH.md

# Existing schema files to modify
@packages/convex/convex/schema/organizationLinks.ts
@packages/convex/convex/schema/users.ts
@packages/convex/convex/schema/validators.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parentBetterAuthOrgId to organizationLinks schema</name>
  <files>packages/convex/convex/schema/organizationLinks.ts</files>
  <action>
Update organizationLinks table to add hierarchy tracking:

1. Add `parentBetterAuthOrgId: v.optional(v.string())` field after the entity ID fields
2. Add new index: `.index("by_parent_org", ["parentBetterAuthOrgId"])`

This enables "all organizations under this treater" queries without joining through domain tables. For treaters, this field is undefined (they are top-level). For generators/haulers, it contains the parent treater's Better Auth org ID.

Keep existing fields and indexes unchanged.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - should pass
File contains `parentBetterAuthOrgId` field and `by_parent_org` index
  </verify>
  <done>
organizationLinks schema has parentBetterAuthOrgId field with index for hierarchy queries
  </done>
</task>

<task type="auto">
  <name>Task 2: Add betterAuthUserId to users schema</name>
  <files>packages/convex/convex/schema/users.ts</files>
  <action>
Update users table to link to Better Auth users:

1. Add `betterAuthUserId: v.optional(v.string())` field after the role field
2. Add new index: `.index("by_better_auth_user", ["betterAuthUserId"])`

This enables resolving domain user from authenticated Better Auth session. The field is optional to support:
- Existing users who haven't been migrated
- Users created before Better Auth integration

Keep existing fields and indexes unchanged.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - should pass
File contains `betterAuthUserId` field and `by_better_auth_user` index
  </verify>
  <done>
users schema has betterAuthUserId field with index for Better Auth user lookups
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy schema and verify</name>
  <files>packages/convex/convex/schema/index.ts</files>
  <action>
Verify schema exports and deploy:

1. Confirm organizationLinks is exported from schema/index.ts (it's not currently - add if missing)
2. Run `pnpm --filter @hwm/convex dev` to sync schema to Convex
3. Verify deployment succeeds without errors

If organizationLinks is not exported from index.ts, add:
```typescript
export { organizationLinks, organizationType } from "./organizationLinks";
```

Note: Schema changes are additive (new optional fields + new indexes) so they're backward compatible and don't require migration.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex dev` - schema syncs successfully
Check Convex dashboard shows updated tables with new fields
  </verify>
  <done>
Schema deployed with parentBetterAuthOrgId on organizationLinks and betterAuthUserId on users
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @hwm/convex typecheck` passes
2. `pnpm --filter @hwm/convex dev` syncs schema successfully
3. organizationLinks table has: betterAuthOrgId, organizationType, treaterId, generatorId, haulerId, parentBetterAuthOrgId, createdAt + 5 indexes
4. users table has: name, email, phone, role, betterAuthUserId, treaterId, generatorId, haulerId, isActive, createdAt, updatedAt + 6 indexes
</verification>

<success_criteria>
- Schema files updated with new fields
- No TypeScript errors
- Schema deployed to Convex
- Tables queryable with new indexes
</success_criteria>

<output>
After completion, create `.planning/phases/02-organization-bridge/02-01-SUMMARY.md`
</output>
