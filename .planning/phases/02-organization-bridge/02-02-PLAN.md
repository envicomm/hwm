---
phase: 02-organization-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/convex/convex/organizations/helpers.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Can resolve domain entity from Better Auth org ID"
    - "Can resolve Better Auth org ID from domain entity"
    - "Can create type-safe organization link data"
    - "Domain user can be retrieved by Better Auth user ID"
  artifacts:
    - path: "packages/convex/convex/organizations/helpers.ts"
      provides: "Organization resolution utilities"
      exports: ["getDomainEntityFromOrg", "getBetterAuthOrgFromEntity", "makeOrgLinkData", "getDomainUser"]
  key_links:
    - from: "packages/convex/convex/organizations/helpers.ts"
      to: "organizationLinks table"
      via: "ctx.db.query with indexes"
      pattern: "withIndex.*by_better_auth_org|by_treater|by_generator|by_hauler"
---

<objective>
Create organization resolution helper utilities.

Purpose: Provide bidirectional lookup between Better Auth organizations and HWM domain entities. These helpers will be used by all organization-scoped queries and mutations.

Output: Helper module with type-safe resolution functions and link creation utilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-organization-bridge/02-CONTEXT.md
@.planning/phases/02-organization-bridge/02-RESEARCH.md

# Reference existing patterns
@packages/convex/convex/lib/auth.ts
@packages/convex/convex/organizationLinks/queries.ts
@packages/convex/convex/schema/organizationLinks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create organizations/helpers.ts with resolution utilities</name>
  <files>packages/convex/convex/organizations/helpers.ts</files>
  <action>
Create new file `packages/convex/convex/organizations/helpers.ts` with:

1. **getDomainEntityFromOrg** - From Better Auth org ID to domain entity:
   - Query organizationLinks by betterAuthOrgId
   - Switch on organizationType to fetch correct entity (treater/generator/hauler)
   - Return { link, entity, type } or throw ConvexError if not found

2. **getBetterAuthOrgFromEntity** - From domain entity to Better Auth org:
   - Accept entityType ("treater" | "generator" | "hauler") and entityId
   - Query organizationLinks using appropriate index (by_treater, by_generator, by_hauler)
   - Return link or throw ConvexError if not found

3. **makeOrgLinkData** - Type-safe link creation helper:
   - Generic function that enforces type-to-field mapping
   - Prevents storing generatorId in treaterId field (common pitfall from RESEARCH.md)
   - Returns object ready for ctx.db.insert

4. **getDomainUser** - Get Convex user from Better Auth user ID:
   - Query users by betterAuthUserId index
   - Return user or null (don't throw - user may not exist yet)

Import types from `../_generated/server` and `../_generated/dataModel`.
Use ConvexError from `convex/values` for errors.

Reference Pattern 3 and Pattern 4 from 02-RESEARCH.md for implementation details.
  </action>
  <verify>
Run `pnpm --filter @hwm/convex typecheck` - should pass
File exports all 4 functions
  </verify>
  <done>
Resolution helpers provide bidirectional org lookup + type-safe link creation + user resolution
  </done>
</task>

<task type="auto">
  <name>Task 2: Create organizations/index.ts barrel export</name>
  <files>packages/convex/convex/organizations/index.ts</files>
  <action>
Create barrel export file `packages/convex/convex/organizations/index.ts`:

```typescript
export * from "./helpers";
```

This follows the existing pattern in other modules (generators/index.ts, haulers/index.ts).
Later plans will add mutations.ts and queries.ts exports here.
  </action>
  <verify>
File exists and exports helpers
Run `pnpm --filter @hwm/convex typecheck` - should pass
  </verify>
  <done>
organizations module has barrel export for helpers
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @hwm/convex typecheck` passes
2. helpers.ts exports: getDomainEntityFromOrg, getBetterAuthOrgFromEntity, makeOrgLinkData, getDomainUser
3. Functions use correct indexes for queries
4. makeOrgLinkData enforces type-to-field mapping at type level
</verification>

<success_criteria>
- All 4 helper functions implemented with proper types
- No TypeScript errors
- Barrel export configured
- Functions query organizationLinks using indexes (not table scans)
</success_criteria>

<output>
After completion, create `.planning/phases/02-organization-bridge/02-02-SUMMARY.md`
</output>
